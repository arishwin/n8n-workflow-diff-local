# Local n8n Workflow Diff

Static, client-side n8n workflow diff viewer.

- No backend.
- No analytics.
- No network calls from the app.
- Supports `#diff=...` URL fragments for shareable review links.

Open `index.html` directly or serve this directory with any static file server.
